{
  "name": "ordiss-automation",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:headed": "npx playwright test --headed",
    "test:debug": "PWDEBUG=1 npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "csv-parser": "^3.2.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
